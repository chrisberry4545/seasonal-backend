version: "3"
services:
  app:
    build: .
    depends_on:
      - postgres
    environment:
      DB_CONNECTION_STRING: postgres://user:pass@postgres:5432/db
      NODE_ENV: development
      PORT: 3000
      DEFAULT_COUNTRY_ID: 9061f3aa-3bf3-4c12-b6f9-4a8d1ea88601
    ports:
      - "5200:3000"
    command: npm run dev

  postgres:
    image: postgres:11.6
    ports:
      - "35432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: db
    volumes:
      - "./seed-db:/docker-entrypoint-initdb.d/"
